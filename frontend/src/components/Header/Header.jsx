import "./Header.scss";

const Header = () => {
    return (
        <header className="header">
            <h2 className="logo">MERN Stack Todo App</h2>
        </header>
    );
};

export default Header;
